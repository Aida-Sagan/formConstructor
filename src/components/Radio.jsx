import React from 'react';

const Radio = ({ label }) => {
    return (
        <div>
            <input type="radio" />
            <label>{label}</label>
        </div>
    );
};

export default Radio;
